/**
 * Placeholder model.
 * This file was accidentally created during an earlier attempt.
 * The real Classes implementation should define its own model(s) after DB schema is added.
 */
export type Classroom = Record<string, never>;
