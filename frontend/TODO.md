# TODO

## Build role-based routes system
- [x] Implement `src/routes.tsx` as the single source of truth for route definitions (paths + elements + role guards + layouts).
- [x] Update `src/App.tsx` to consume the route definitions from `src/routes.tsx`.
- [x] Add `/parent/*` route scaffolding with a placeholder page (best option for system) and wire role guard.
- [x] Ensure Sidebar links match the final route paths.
- [x] Run `npm run build` (and/or `npm run dev`) in `frontend` to verify TypeScript and routing compile.


